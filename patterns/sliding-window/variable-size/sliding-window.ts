/**
 * Applies a variable size sliding window over an array
 * @param {number[]} nums - The input array
 * @returns {void}
 */
export function slidingWindow(nums: number[]): void {
  let low = 0;

  for (let high = 0; high < nums.length; high++) {
    // add nums[high] to the window

    while (true /* replace true with some condition */) {
      // remove nums[low] from the window till window is invalid
      low++;
    }

    // update the answer
  }
}
